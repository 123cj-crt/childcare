# 智慧托育系统 - 项目结构说明

## 📁 项目目录结构

```
智慧托育/
├── 📱 前端（微信小程序）/
│   ├── app.js                 # 小程序入口文件
│   ├── app.json              # 小程序配置
│   ├── app.wxss              # 全局样式
│   ├── pages/                # 页面文件
│   │   ├── index/            # 首页
│   │   ├── login/            # 登录页
│   │   ├── schedule/         # 课表页
│   │   ├── notice/           # 通知页
│   │   ├── profile/          # 个人中心
│   │   ├── course-detail/    # 课程详情
│   │   └── bindchild/        # 绑定儿童
│   ├── utils/                # 工具函数
│   ├── icon/                 # 图标资源
│   └── images/               # 图片资源
├── 🖥️ 智慧托育后端/          # 后端系统文件夹
│   ├── backend/              # 后端代码
│   │   ├── app.js            # 主应用文件
│   │   ├── config.js         # 配置文件
│   │   ├── package.json      # 依赖管理
│   │   ├── start.js          # 启动脚本
│   │   ├── test-api.js       # API测试脚本
│   │   ├── database/         # 数据库相关
│   │   │   ├── connection.js # 数据库连接
│   │   │   └── schema.sql    # 数据库结构
│   │   ├── middleware/       # 中间件
│   │   │   ├── auth.js       # 认证中间件
│   │   │   ├── cors.js       # 跨域中间件
│   │   │   ├── rateLimit.js  # 限流中间件
│   │   │   └── errorHandler.js # 错误处理中间件
│   │   ├── routes/           # 路由文件
│   │   │   ├── auth.js       # 认证路由
│   │   │   ├── users.js      # 用户路由
│   │   │   ├── courses.js    # 课程路由
│   │   │   ├── schedules.js  # 课表路由
│   │   │   └── notifications.js # 通知路由
│   │   ├── services/         # 服务层
│   │   │   └── wechatService.js # 微信服务
│   │   ├── scripts/          # 脚本文件
│   │   │   └── init-database.js # 数据库初始化
│   │   ├── Dockerfile        # Docker配置
│   │   ├── docker-compose.yml # Docker Compose配置
│   │   ├── nginx.conf        # Nginx配置
│   │   └── README.md         # 后端文档
│   └── 项目说明.md           # 完整项目说明
└── 📋 项目文件结构.md        # 原始结构说明
```

## 🚀 快速开始

### 1. 前端开发
- 用微信开发者工具打开项目根目录
- 所有前端文件都在根目录下

### 2. 后端开发
```bash
# 进入后端目录
cd 智慧托育后端/backend

# 安装依赖
npm install

# 配置数据库（修改 config.js）
# 初始化数据库
node scripts/init-database.js

# 启动服务
node start.js
```

### 3. 测试API
```bash
cd 智慧托育后端/backend
node test-api.js
```

## 📚 文档说明

- `智慧托育后端/项目说明.md` - 完整项目说明和API文档
- `智慧托育后端/backend/README.md` - 后端详细文档
- `智慧托育后端/backend/DEPLOYMENT.md` - 部署指南
- `项目结构说明.md` - 本文件，项目结构说明

## 🔧 配置要点

1. **前端配置**：修改 `pages/login/login.js` 中的API地址
2. **后端配置**：修改 `智慧托育后端/backend/config.js` 中的数据库和微信配置
3. **数据库**：使用 `智慧托育后端/backend/database/schema.sql` 初始化数据库

## 📞 技术支持

如有问题，请参考各文档文件或联系开发团队。

